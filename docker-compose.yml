services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    env_file: .env
    volumes:
      - ./backend/data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
